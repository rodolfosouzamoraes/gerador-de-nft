using UnityEngine;

public class MainCameraCtlr : MonoBehaviour
{
    public RenderTexture render;
    public Camera mainCamera;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //mainCamera.targetTexture = render;
    }
}
